import { SelectedOffrePipe } from './selected-offre.pipe';

describe('SelectedOffrePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedOffrePipe();
    expect(pipe).toBeTruthy();
  });
});
